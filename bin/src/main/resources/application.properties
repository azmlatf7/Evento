# MongoDB connection (update URI if using cloud)
spring.data.mongodb.uri=mongodb://localhost:27017/Evento

# Thymeleaf
spring.thymeleaf.cache=false

# Security (optional - for testing dev login)
spring.security.user.name=admin
spring.security.user.password=admin123
